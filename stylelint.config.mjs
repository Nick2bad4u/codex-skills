import sharedConfig from "stylelint-config-nick2bad4u";

/** @type {import("stylelint").Config} */
const stylelintConfig = /** @type {import("stylelint").Config} */ (
    sharedConfig
);

export default stylelintConfig;
