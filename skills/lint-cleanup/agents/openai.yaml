# yaml-language-server: $schema=https://json.schemastore.org/codex-skill-metadata.json
interface:
    brand_color: "#DC2626"
    default_prompt: "Use $lint-cleanup to fix the lint issues and verify a clean rerun."
    display_name: "Lint Cleanup"
    icon_large: "./assets/lint-cleanup.svg"
    icon_small: "./assets/lint-cleanup-small.svg"
    short_description: "Fix lint failures without shortcuts"
