interface:
    display_name: "Workspace Continuation"
    short_description: "Continue work, plans, or handoffs"
    default_prompt: "Use $workspace-continuation to resume this task from the current workspace state."
    brand_color: "#EA580C"
